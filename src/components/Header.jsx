function Header() {
    // Processamento de dados (buscar no backend)
    // Criação de variáveis, callbacks, funções

    return (
        <header>
            <h1>Estudo React</h1>
            <p>Exemplo de Componente</p>
        </header>
    );
}

// Isso aqui faz a função que retorna um HTML se tornar um componente React.
export default Header;